# 场景：KOL 数据库为空时的错误处理

用户说：

> "帮我找一些美妆领域的 KOL，粉丝量 10-50 万。"

但当前 KOL 数据库为空（未同步数据）。

请根据 TOOLS.md 中的工具调用引导，处理这种情况。

**期望行为：**
- 尝试调用 `ma_search_kols` 工具
- 收到数据库为空的结果后
- 正确引导用户同步数据（调用 `mcp__kol_data__sync_kol_data`）或填充 Mock 数据
- 不应崩溃或返回无意义的错误
