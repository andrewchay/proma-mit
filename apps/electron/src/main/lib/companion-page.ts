/**
 * Companion 移动端单页（Task 7 替换为完整实现）
 *
 * 单文件 HTML，内联 CSS/JS，由 companion-server 直接返回；页面本身不含任何数据。
 */

export function getCompanionPageHtml(): string {
  return `<!doctype html>
<html lang="zh-CN">
<head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">
<title>Gravitas Companion</title></head>
<body style="font-family:-apple-system,sans-serif;background:#0d1117;color:#e6edf3;padding:24px">
<h1>Gravitas Companion</h1>
<p>完整界面建设中（占位页）。</p>
</body></html>`
}
